# Bank statements — real financial data, never commit
docs/samples/statement-sample.csv
*statement*.csv
*.ofx

# Saved supplier pages used to test the extractor. Large, contain the
# supplier's content and a browser session token from your visit, and are not
# ours to republish. Keep them locally; tests skip when absent.
docs/samples/*.html
docs/samples/*_files/

# Secrets
.env
.env.*
!.env.example

# Staged extension captures (local review queue)
data/captured.json

# Editor rollback copy
data/products.json.bak

# Build output
dist/
node_modules/

# OS
.DS_Store
